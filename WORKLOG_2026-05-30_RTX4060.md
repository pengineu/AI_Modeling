# 작업 로그 — 2026-05-30 (RTX 4060 GPU 머신)

> 이 머신의 역할: **모델 학습·검증**. 코드 작성은 다른 PC에서 진행, git(main)으로 동기화.
> 이 문서는 이번 세션에서 수행한 전 과정을 정리한 것. 모델 **구조는 변경하지 않음**(제출 호환 유지).

## 요약 (한눈에)
| 단계 | 상태 | 산출물 / 결과 |
|---|---|---|
| 0. 환경 점검 | ✅ 완료 | CPU torch → CUDA 빌드 교체, CUDA True |
| 1. 데이터 준비 | ✅ 완료 | `data/train` 7200장, 라벨 분포 균형 확인 |
| 2. Task1 분류 학습 | ✅ **완료** | `checkpoints/task1.pt` (val style **1.0000** / fruit **0.9993**) |
| 3. Task2 StarGAN 학습 | ❌ **실패(모드 붕괴)** → 재학습 대기 | 코드 수정 후 재시도 필요 (별도 보고서 참조) |
| 4. 업로드 & 기입 | ⏳ 대기 | Task2 완료 후 진행 |

---

## 0. 환경 점검
- 초기 상태: `torch 2.6.0+cpu`(CPU 전용) → `torch.cuda.is_available() == False`, `gdown` 미설치.
- 조치:
  ```
  pip uninstall -y torch torchvision
  pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
  pip install gdown
  ```
- 결과: **torch 2.5.1+cu121, CUDA True, RTX 4060 인식**, CPU·CUDA backward 모두 정상. pillow 12.1.0.
- 환경: Python 3.12.9(전역), 드라이버 581.57, GPU 8GB.

## 1. 데이터 준비
- gdown(`id=14Yc5eDlrEvx4wWp6gtSiJ52ne_noEl9u`)은 **Google Drive 다운로드 할당량 초과**로 실패("Too many users...", 최대 24h 지속).
- 대신 머신에 이미 존재하는 데이터 발견·활용:
  - 압축본: `C:\Users\sinja\Downloads\train.zip` (3.48GB)
  - 해제본: `D:\coding\Term_Project(2026-1)\train\` (`images/` 7200장 + `train_labels.csv`)
- 프로젝트 경로 연결(재복사 회피): `data\train` → 위 해제본으로 **디렉터리 정션**.
  ```
  New-Item -ItemType Junction -Path data\train -Target "D:\coding\Term_Project(2026-1)\train"
  ```
- 검증 `python -m src.common.dataset data/train`:
  - **images=7200, labeled=7200**
  - fruit: apple/asian pear/banana/cherry/grape/pineapple 각 **1200** (6×1200)
  - style: pencil color/oil painting/water color 각 **2400** (3×2400)
  - → 확정 구조와 일치, 균형 정상.

## 2. Task1 분류 학습 — ✅ 완료
- 명령: `python -m src.task1.train --data data/train --epochs 15 --batch 64 --out checkpoints/task1.pt`
- device cuda, train=5760 / val=1440, EfficientNet-B0 pretrained 사용.
- 결과 (epoch별 val):
  ```
  epoch 01  fruit 0.9972  style 0.9972
  epoch 05  fruit 0.9986  style 1.0000
  epoch 06  fruit 0.9993  style 1.0000   <- best mean 0.9997
  epoch 15  fruit 0.9986  style 1.0000
  done. best mean acc: 0.99965
  ```
- **best 체크포인트 저장**: `checkpoints/task1.pt` (15.6MB) — val **style acc 1.0000**, **fruit acc 0.9993**. 목표 달성.
- 이 체크포인트는 Task1·Task3 노트북 양쪽에서 사용(업로드 후 ID 기입 필요).

## 3. Task2 StarGAN 학습 — ❌ 모드 붕괴 (재학습 대기)
- 동작 확인(smoke) `--iters 2000 --batch 16 --img-size 128`: **성공** — 샘플(`samples_task2/iter_002000.png`, `iter_004000.png`)에서 콘텐츠 보존 + 스타일 변환 양호, GPU 4.5GB/8GB(batch 16 안전).
- 본 학습 `--iters 200000 --batch 16` (세션과 독립된 detached 프로세스로 실행):
  - iter ~5k–14.3k 안정(D~0.2, G_adv~2, G_rec~0.15–0.20), 속도 4.7 it/s(ETA ~11h).
  - **iter ~14,400 불안정 스파이크 → 회복 실패 → iter 16,000 모드 붕괴 고착**(D→0, G_adv 폭발, 샘플이 동일한 초록 덩어리로 붕괴).
  - → 프로세스 중단.
- 근본 원인: 판별자에 **Lipschitz 제약 없음**(no spectral norm/GP) + **hinge loss** + **D:G 5:1**. 또한 체크포인트가 같은 파일을 덮어써 정상 가중치(iter 10k) 손실.
- **상세·제안 수정은 [TASK2_COLLAPSE_REPORT.md](TASK2_COLLAPSE_REPORT.md) 참조** (R1/spectral norm, n_critic 조정, 태그 저장 등 — 모두 구조 변경 없음, 코드 담당 PC에서 진행).

## 4. 현재 체크포인트 상태
- `checkpoints/task1.pt` — ✅ 정상, 업로드 가능.
- `checkpoints/stargan_G.pt` — ⚠️ 붕괴 직전(iter 15k) 상태. **사용 금지**, 재학습으로 교체 예정.

## 5. 다음 단계
1. **코드 담당 PC**: `src/task2/train.py`(+필요시 D에 spectral_norm) 안정화 수정 → git push.
2. **이 머신**: `git pull` → Task2 재학습(`--iters 200000 --batch 16 --img-size 128`). 초반 샘플(iter 2k·4k…) 조기·빈번 점검으로 붕괴 조짐 즉시 감지.
3. Task2 정상 완료 후: `task1.pt`, `stargan_G.pt` → Google Drive 업로드("링크 있는 모든 사용자") → 파일 ID를 `Termproject_202502204_Task{1,2,3}_model.txt` 및 노트북 `*_FILE_ID`에 기입.
4. Colab 새 런타임에서 노트북 3개 전체 실행 검증.

## 6. 재현용 핵심 명령
```bash
# 환경 확인
python -c "import torch; print(torch.__version__, torch.cuda.is_available())"   # True 필수
python -m src.common.dataset data/train                                          # images=7200

# Task1 (완료됨, 필요시 재학습)
python -m src.task1.train --data data/train --epochs 15 --batch 64 --out checkpoints/task1.pt

# Task2 (코드 수정 후 재학습)
python -m src.task2.train --data data/train --iters 2000   --batch 16 --img-size 128   # smoke
python -m src.task2.train --data data/train --iters 200000 --batch 16 --img-size 128   # 본 학습
```

## 참고 (이 머신 특이사항)
- 데이터는 로컬에 있으니 gdown 재시도 불필요(할당량 막힘). `data\train` 정션만 확인/생성.
- 학습은 GPU 필수 — 세션 시작 시 `cuda.is_available()` 확인하고 False면 cu121로 재설치.
